#foreach($failureAnalysis in $problemItem.events)
<div class="probtypeinfo">
   A failure indicated by $failureAnalysis.failure.summary.
</div>
<div class="problem">
   <div class="table">
      #set( $simpleCounter = $simpleCounter + 1 )
      <a onclick="javascript:showDetails('failstacktrace-$simpleCounter',2)">
         <img id="image-failstacktrace-$simpleCounter" src="images/more-detail16.jpg"></a>
      <table class="stacktrace" id="failstacktrace-$simpleCounter" border="1" cellpadding="4" cellspacing="1" width="95%" ALIGN="center">
         <tbody>
            <tr class="stacktrace">
               <th class="stacktrace">Class</th>
               <th class="stacktrace">Method</th>
               <th class="stacktrace">Line</th>
            </tr>
            #foreach($frame in $failureAnalysis.failure.threadState.stackTrace)
               #if($velocityCount > 3)
                  <tr style="display:none;" class="stacktrace">             
               #else
                  <tr class="stacktrace">
               #end
                     <td class="stacktrace">$frame.className</td>
                     <td class="stacktrace">$frame.methodName</td>
                     <td class="stacktrace">$display.formatStackTraceLine($frame.lineNumber)</td>
                  </tr>
            #end
         </tbody>
      </table>
   </div>
</div>
     
#set($hasParam = false)
#foreach($url in $failureAnalysis.events)
   #set($hasParam = $hasParam || ($url.parameterString != "" && $url.parameterString != "(no parameters)"))
#end
   
<br/>
#if($hasParam)
   This table provides a list of the parameters associated with the last $failureAnalysis.events.size() problems.
   
   <div class="table">
      <table class="sql" border="1" cellspacing="1" cellpadding="4">	    
	         <tr class="sql">
	           <th class="sql">Parameters</th>
	         </tr>
	         #foreach($url in $failureAnalysis.events)
		            <tr class="sql">
		              <td class="sql">$url.parameterString</td>
		            </tr>
	         #end
      </table>
   </div>
#end


#end