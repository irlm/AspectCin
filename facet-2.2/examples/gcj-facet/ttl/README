README
------

This directory contains a simple example on how to use the GCJ and CNI
route to write C++ code which uses FACET for event delivery. The trick
here is that FACET itself has already been compiled to native code and
so all code being run finally is in the native form thus incurring
zero overhead.



